{
  "name": "tdilearned-api-gateway",
  "version": "1.0.0",
  "description": "Edge API gateway: Cloudflare Durable Object rate limiting + reverse proxy to the Azure backend",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250204.0",
    "typescript": "^5.7.3",
    "wrangler": "^3.107.2"
  }
}